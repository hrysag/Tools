export enum GameState {
    NULL,
    BEGIN,
    NORMAL,
    RE_SPINE,
    FREE_GAME,
}

export enum TransitionsState {
    IN,
    OUT,
    NONE
}

export enum ShowBottomTextStatus {
    NO_WIN,
    ROLLING,
    WIN,
    IDLE,
    DEBUG
}
