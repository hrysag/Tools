# Web_Slot

## Intro

1. 安裝 node.js [連結](https://nodejs.org/en/download/current)
2. 開啟終端執行指令 >`npm install`，安裝npm套件
3. 安裝vscode workspace 推薦的插件

使用預先設定的task安裝插件:
按下`ctrl`+`shift`+`p`，輸入`Tasks: Run Task` > `Install All Recommended Extensions`
